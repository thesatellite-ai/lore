// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	stdsql "database/sql"
	"fmt"
	"sync"

	"entgo.io/ent/dialect"
)

// Tx is a transactional client that is created by calling Client.Tx().
type Tx struct {
	config
	// ActivityArchive is the client for interacting with the ActivityArchive builders.
	ActivityArchive *ActivityArchiveClient
	// Actor is the client for interacting with the Actor builders.
	Actor *ActorClient
	// ArchitectureNote is the client for interacting with the ArchitectureNote builders.
	ArchitectureNote *ArchitectureNoteClient
	// AssembleCitation is the client for interacting with the AssembleCitation builders.
	AssembleCitation *AssembleCitationClient
	// AssembleRun is the client for interacting with the AssembleRun builders.
	AssembleRun *AssembleRunClient
	// AuditLog is the client for interacting with the AuditLog builders.
	AuditLog *AuditLogClient
	// Behaviour is the client for interacting with the Behaviour builders.
	Behaviour *BehaviourClient
	// BenchEval is the client for interacting with the BenchEval builders.
	BenchEval *BenchEvalClient
	// BenchResult is the client for interacting with the BenchResult builders.
	BenchResult *BenchResultClient
	// BenchRun is the client for interacting with the BenchRun builders.
	BenchRun *BenchRunClient
	// CodeFile is the client for interacting with the CodeFile builders.
	CodeFile *CodeFileClient
	// CodeSymbol is the client for interacting with the CodeSymbol builders.
	CodeSymbol *CodeSymbolClient
	// Comment is the client for interacting with the Comment builders.
	Comment *CommentClient
	// CommitLink is the client for interacting with the CommitLink builders.
	CommitLink *CommitLinkClient
	// CompressRun is the client for interacting with the CompressRun builders.
	CompressRun *CompressRunClient
	// CookbookRecipe is the client for interacting with the CookbookRecipe builders.
	CookbookRecipe *CookbookRecipeClient
	// DBConfig is the client for interacting with the DBConfig builders.
	DBConfig *DBConfigClient
	// Decision is the client for interacting with the Decision builders.
	Decision *DecisionClient
	// Draft is the client for interacting with the Draft builders.
	Draft *DraftClient
	// EntityTag is the client for interacting with the EntityTag builders.
	EntityTag *EntityTagClient
	// ExternalSource is the client for interacting with the ExternalSource builders.
	ExternalSource *ExternalSourceClient
	// Handoff is the client for interacting with the Handoff builders.
	Handoff *HandoffClient
	// Hotfix is the client for interacting with the Hotfix builders.
	Hotfix *HotfixClient
	// IdentityProfile is the client for interacting with the IdentityProfile builders.
	IdentityProfile *IdentityProfileClient
	// Incident is the client for interacting with the Incident builders.
	Incident *IncidentClient
	// InterventionMetric is the client for interacting with the InterventionMetric builders.
	InterventionMetric *InterventionMetricClient
	// KnowledgeRef is the client for interacting with the KnowledgeRef builders.
	KnowledgeRef *KnowledgeRefClient
	// KnowledgeRevision is the client for interacting with the KnowledgeRevision builders.
	KnowledgeRevision *KnowledgeRevisionClient
	// LearnCandidate is the client for interacting with the LearnCandidate builders.
	LearnCandidate *LearnCandidateClient
	// LearnRun is the client for interacting with the LearnRun builders.
	LearnRun *LearnRunClient
	// Memory is the client for interacting with the Memory builders.
	Memory *MemoryClient
	// MemoryCodeRef is the client for interacting with the MemoryCodeRef builders.
	MemoryCodeRef *MemoryCodeRefClient
	// Mission is the client for interacting with the Mission builders.
	Mission *MissionClient
	// MountAlias is the client for interacting with the MountAlias builders.
	MountAlias *MountAliasClient
	// Pattern is the client for interacting with the Pattern builders.
	Pattern *PatternClient
	// PiiPattern is the client for interacting with the PiiPattern builders.
	PiiPattern *PiiPatternClient
	// Plan is the client for interacting with the Plan builders.
	Plan *PlanClient
	// Playbook is the client for interacting with the Playbook builders.
	Playbook *PlaybookClient
	// Project is the client for interacting with the Project builders.
	Project *ProjectClient
	// ProjectConfig is the client for interacting with the ProjectConfig builders.
	ProjectConfig *ProjectConfigClient
	// Prompt is the client for interacting with the Prompt builders.
	Prompt *PromptClient
	// QueryLog is the client for interacting with the QueryLog builders.
	QueryLog *QueryLogClient
	// Reminder is the client for interacting with the Reminder builders.
	Reminder *ReminderClient
	// RenderHistory is the client for interacting with the RenderHistory builders.
	RenderHistory *RenderHistoryClient
	// Repo is the client for interacting with the Repo builders.
	Repo *RepoClient
	// Rule is the client for interacting with the Rule builders.
	Rule *RuleClient
	// RuleVerifierRef is the client for interacting with the RuleVerifierRef builders.
	RuleVerifierRef *RuleVerifierRefClient
	// Run is the client for interacting with the Run builders.
	Run *RunClient
	// RunStep is the client for interacting with the RunStep builders.
	RunStep *RunStepClient
	// SchemaMigration is the client for interacting with the SchemaMigration builders.
	SchemaMigration *SchemaMigrationClient
	// Session is the client for interacting with the Session builders.
	Session *SessionClient
	// Snapshot is the client for interacting with the Snapshot builders.
	Snapshot *SnapshotClient
	// Suggestion is the client for interacting with the Suggestion builders.
	Suggestion *SuggestionClient
	// Tag is the client for interacting with the Tag builders.
	Tag *TagClient
	// Task is the client for interacting with the Task builders.
	Task *TaskClient
	// TaskList is the client for interacting with the TaskList builders.
	TaskList *TaskListClient
	// TaskView is the client for interacting with the TaskView builders.
	TaskView *TaskViewClient
	// TastePref is the client for interacting with the TastePref builders.
	TastePref *TastePrefClient
	// TechDoc is the client for interacting with the TechDoc builders.
	TechDoc *TechDocClient
	// TechDocPage is the client for interacting with the TechDocPage builders.
	TechDocPage *TechDocPageClient
	// TrustedPlugin is the client for interacting with the TrustedPlugin builders.
	TrustedPlugin *TrustedPluginClient
	// User is the client for interacting with the User builders.
	User *UserClient
	// Workflow is the client for interacting with the Workflow builders.
	Workflow *WorkflowClient
	// Workspace is the client for interacting with the Workspace builders.
	Workspace *WorkspaceClient

	// lazily loaded.
	client     *Client
	clientOnce sync.Once
	// ctx lives for the life of the transaction. It is
	// the same context used by the underlying connection.
	ctx context.Context
}

type (
	// Committer is the interface that wraps the Commit method.
	Committer interface {
		Commit(context.Context, *Tx) error
	}

	// The CommitFunc type is an adapter to allow the use of ordinary
	// function as a Committer. If f is a function with the appropriate
	// signature, CommitFunc(f) is a Committer that calls f.
	CommitFunc func(context.Context, *Tx) error

	// CommitHook defines the "commit middleware". A function that gets a Committer
	// and returns a Committer. For example:
	//
	//	hook := func(next ent.Committer) ent.Committer {
	//		return ent.CommitFunc(func(ctx context.Context, tx *ent.Tx) error {
	//			// Do some stuff before.
	//			if err := next.Commit(ctx, tx); err != nil {
	//				return err
	//			}
	//			// Do some stuff after.
	//			return nil
	//		})
	//	}
	//
	CommitHook func(Committer) Committer
)

// Commit calls f(ctx, m).
func (f CommitFunc) Commit(ctx context.Context, tx *Tx) error {
	return f(ctx, tx)
}

// Commit commits the transaction.
func (tx *Tx) Commit() error {
	txDriver := tx.config.driver.(*txDriver)
	var fn Committer = CommitFunc(func(context.Context, *Tx) error {
		return txDriver.tx.Commit()
	})
	txDriver.mu.Lock()
	hooks := append([]CommitHook(nil), txDriver.onCommit...)
	txDriver.mu.Unlock()
	for i := len(hooks) - 1; i >= 0; i-- {
		fn = hooks[i](fn)
	}
	return fn.Commit(tx.ctx, tx)
}

// OnCommit adds a hook to call on commit.
func (tx *Tx) OnCommit(f CommitHook) {
	txDriver := tx.config.driver.(*txDriver)
	txDriver.mu.Lock()
	txDriver.onCommit = append(txDriver.onCommit, f)
	txDriver.mu.Unlock()
}

type (
	// Rollbacker is the interface that wraps the Rollback method.
	Rollbacker interface {
		Rollback(context.Context, *Tx) error
	}

	// The RollbackFunc type is an adapter to allow the use of ordinary
	// function as a Rollbacker. If f is a function with the appropriate
	// signature, RollbackFunc(f) is a Rollbacker that calls f.
	RollbackFunc func(context.Context, *Tx) error

	// RollbackHook defines the "rollback middleware". A function that gets a Rollbacker
	// and returns a Rollbacker. For example:
	//
	//	hook := func(next ent.Rollbacker) ent.Rollbacker {
	//		return ent.RollbackFunc(func(ctx context.Context, tx *ent.Tx) error {
	//			// Do some stuff before.
	//			if err := next.Rollback(ctx, tx); err != nil {
	//				return err
	//			}
	//			// Do some stuff after.
	//			return nil
	//		})
	//	}
	//
	RollbackHook func(Rollbacker) Rollbacker
)

// Rollback calls f(ctx, m).
func (f RollbackFunc) Rollback(ctx context.Context, tx *Tx) error {
	return f(ctx, tx)
}

// Rollback rollbacks the transaction.
func (tx *Tx) Rollback() error {
	txDriver := tx.config.driver.(*txDriver)
	var fn Rollbacker = RollbackFunc(func(context.Context, *Tx) error {
		return txDriver.tx.Rollback()
	})
	txDriver.mu.Lock()
	hooks := append([]RollbackHook(nil), txDriver.onRollback...)
	txDriver.mu.Unlock()
	for i := len(hooks) - 1; i >= 0; i-- {
		fn = hooks[i](fn)
	}
	return fn.Rollback(tx.ctx, tx)
}

// OnRollback adds a hook to call on rollback.
func (tx *Tx) OnRollback(f RollbackHook) {
	txDriver := tx.config.driver.(*txDriver)
	txDriver.mu.Lock()
	txDriver.onRollback = append(txDriver.onRollback, f)
	txDriver.mu.Unlock()
}

// Client returns a Client that binds to current transaction.
func (tx *Tx) Client() *Client {
	tx.clientOnce.Do(func() {
		tx.client = &Client{config: tx.config}
		tx.client.init()
	})
	return tx.client
}

func (tx *Tx) init() {
	tx.ActivityArchive = NewActivityArchiveClient(tx.config)
	tx.Actor = NewActorClient(tx.config)
	tx.ArchitectureNote = NewArchitectureNoteClient(tx.config)
	tx.AssembleCitation = NewAssembleCitationClient(tx.config)
	tx.AssembleRun = NewAssembleRunClient(tx.config)
	tx.AuditLog = NewAuditLogClient(tx.config)
	tx.Behaviour = NewBehaviourClient(tx.config)
	tx.BenchEval = NewBenchEvalClient(tx.config)
	tx.BenchResult = NewBenchResultClient(tx.config)
	tx.BenchRun = NewBenchRunClient(tx.config)
	tx.CodeFile = NewCodeFileClient(tx.config)
	tx.CodeSymbol = NewCodeSymbolClient(tx.config)
	tx.Comment = NewCommentClient(tx.config)
	tx.CommitLink = NewCommitLinkClient(tx.config)
	tx.CompressRun = NewCompressRunClient(tx.config)
	tx.CookbookRecipe = NewCookbookRecipeClient(tx.config)
	tx.DBConfig = NewDBConfigClient(tx.config)
	tx.Decision = NewDecisionClient(tx.config)
	tx.Draft = NewDraftClient(tx.config)
	tx.EntityTag = NewEntityTagClient(tx.config)
	tx.ExternalSource = NewExternalSourceClient(tx.config)
	tx.Handoff = NewHandoffClient(tx.config)
	tx.Hotfix = NewHotfixClient(tx.config)
	tx.IdentityProfile = NewIdentityProfileClient(tx.config)
	tx.Incident = NewIncidentClient(tx.config)
	tx.InterventionMetric = NewInterventionMetricClient(tx.config)
	tx.KnowledgeRef = NewKnowledgeRefClient(tx.config)
	tx.KnowledgeRevision = NewKnowledgeRevisionClient(tx.config)
	tx.LearnCandidate = NewLearnCandidateClient(tx.config)
	tx.LearnRun = NewLearnRunClient(tx.config)
	tx.Memory = NewMemoryClient(tx.config)
	tx.MemoryCodeRef = NewMemoryCodeRefClient(tx.config)
	tx.Mission = NewMissionClient(tx.config)
	tx.MountAlias = NewMountAliasClient(tx.config)
	tx.Pattern = NewPatternClient(tx.config)
	tx.PiiPattern = NewPiiPatternClient(tx.config)
	tx.Plan = NewPlanClient(tx.config)
	tx.Playbook = NewPlaybookClient(tx.config)
	tx.Project = NewProjectClient(tx.config)
	tx.ProjectConfig = NewProjectConfigClient(tx.config)
	tx.Prompt = NewPromptClient(tx.config)
	tx.QueryLog = NewQueryLogClient(tx.config)
	tx.Reminder = NewReminderClient(tx.config)
	tx.RenderHistory = NewRenderHistoryClient(tx.config)
	tx.Repo = NewRepoClient(tx.config)
	tx.Rule = NewRuleClient(tx.config)
	tx.RuleVerifierRef = NewRuleVerifierRefClient(tx.config)
	tx.Run = NewRunClient(tx.config)
	tx.RunStep = NewRunStepClient(tx.config)
	tx.SchemaMigration = NewSchemaMigrationClient(tx.config)
	tx.Session = NewSessionClient(tx.config)
	tx.Snapshot = NewSnapshotClient(tx.config)
	tx.Suggestion = NewSuggestionClient(tx.config)
	tx.Tag = NewTagClient(tx.config)
	tx.Task = NewTaskClient(tx.config)
	tx.TaskList = NewTaskListClient(tx.config)
	tx.TaskView = NewTaskViewClient(tx.config)
	tx.TastePref = NewTastePrefClient(tx.config)
	tx.TechDoc = NewTechDocClient(tx.config)
	tx.TechDocPage = NewTechDocPageClient(tx.config)
	tx.TrustedPlugin = NewTrustedPluginClient(tx.config)
	tx.User = NewUserClient(tx.config)
	tx.Workflow = NewWorkflowClient(tx.config)
	tx.Workspace = NewWorkspaceClient(tx.config)
}

// txDriver wraps the given dialect.Tx with a nop dialect.Driver implementation.
// The idea is to support transactions without adding any extra code to the builders.
// When a builder calls to driver.Tx(), it gets the same dialect.Tx instance.
// Commit and Rollback are nop for the internal builders and the user must call one
// of them in order to commit or rollback the transaction.
//
// If a closed transaction is embedded in one of the generated entities, and the entity
// applies a query, for example: ActivityArchive.QueryXXX(), the query will be executed
// through the driver which created this transaction.
//
// Note that txDriver is not goroutine safe.
type txDriver struct {
	// the driver we started the transaction from.
	drv dialect.Driver
	// tx is the underlying transaction.
	tx dialect.Tx
	// completion hooks.
	mu         sync.Mutex
	onCommit   []CommitHook
	onRollback []RollbackHook
}

// newTx creates a new transactional driver.
func newTx(ctx context.Context, drv dialect.Driver) (*txDriver, error) {
	tx, err := drv.Tx(ctx)
	if err != nil {
		return nil, err
	}
	return &txDriver{tx: tx, drv: drv}, nil
}

// Tx returns the transaction wrapper (txDriver) to avoid Commit or Rollback calls
// from the internal builders. Should be called only by the internal builders.
func (tx *txDriver) Tx(context.Context) (dialect.Tx, error) { return tx, nil }

// Dialect returns the dialect of the driver we started the transaction from.
func (tx *txDriver) Dialect() string { return tx.drv.Dialect() }

// Close is a nop close.
func (*txDriver) Close() error { return nil }

// Commit is a nop commit for the internal builders.
// User must call `Tx.Commit` in order to commit the transaction.
func (*txDriver) Commit() error { return nil }

// Rollback is a nop rollback for the internal builders.
// User must call `Tx.Rollback` in order to rollback the transaction.
func (*txDriver) Rollback() error { return nil }

// Exec calls tx.Exec.
func (tx *txDriver) Exec(ctx context.Context, query string, args, v any) error {
	return tx.tx.Exec(ctx, query, args, v)
}

// Query calls tx.Query.
func (tx *txDriver) Query(ctx context.Context, query string, args, v any) error {
	return tx.tx.Query(ctx, query, args, v)
}

var _ dialect.Driver = (*txDriver)(nil)

// ExecContext allows calling the underlying ExecContext method of the transaction if it is supported by it.
// See, database/sql#Tx.ExecContext for more information.
func (tx *txDriver) ExecContext(ctx context.Context, query string, args ...any) (stdsql.Result, error) {
	ex, ok := tx.tx.(interface {
		ExecContext(context.Context, string, ...any) (stdsql.Result, error)
	})
	if !ok {
		return nil, fmt.Errorf("Tx.ExecContext is not supported")
	}
	return ex.ExecContext(ctx, query, args...)
}

// QueryContext allows calling the underlying QueryContext method of the transaction if it is supported by it.
// See, database/sql#Tx.QueryContext for more information.
func (tx *txDriver) QueryContext(ctx context.Context, query string, args ...any) (*stdsql.Rows, error) {
	q, ok := tx.tx.(interface {
		QueryContext(context.Context, string, ...any) (*stdsql.Rows, error)
	})
	if !ok {
		return nil, fmt.Errorf("Tx.QueryContext is not supported")
	}
	return q.QueryContext(ctx, query, args...)
}

// Package constants centralizes string literals used across the lore
// CLI surface. Each subset lives in its own file:
//
//	entities.go  — Entity (polymorphic entity_table values) + Verb constants
//	envelopes.go — JSON envelope `kind` strings
//	sources.go   — SourceKind (provenance) constants
//	flags.go     — Cobra flag names
package constants
